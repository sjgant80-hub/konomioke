<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base app theme — used after splash resolves -->
    <style name="Theme.Konomioke" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/kon</item>
        <item name="colorPrimaryVariant">@color/mus</item>
        <item name="colorOnPrimary">@color/text_primary</item>
        <item name="android:statusBarColor">@color/bg_deep</item>
        <item name="android:navigationBarColor">@color/bg_deep</item>
        <item name="android:windowBackground">@color/bg_deep</item>
    </style>

    <!-- Splash screen theme (Android 12+ SplashScreen API) -->
    <style name="Theme.Konomioke.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_bg</item>
        <!-- ic_launcher_foreground must be an adaptive icon drawable -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_icon</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/splash_icon_bg</item>
        <item name="postSplashScreenTheme">@style/Theme.Konomioke</item>
    </style>

</resources>
