<?xml version="1.0" encoding="utf-8"?>
<!--
    Network security config.
    Production: HTTPS only to konomioke.com.
    Add a <debug-overrides> block below if you need cleartext to localhost during dev.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <!-- Allow localhost cleartext only in debug builds -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">konomioke.com</domain>
    </domain-config>
</network-security-config>
